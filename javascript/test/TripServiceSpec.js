"use strict";

let assert = require('assert');
let TripService = require('../src/TripService');

describe('TripService', () => {

    it('should... ', () => {
        assert.equal(2+2, 5);
    });

});
